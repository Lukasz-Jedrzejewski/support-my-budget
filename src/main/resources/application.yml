server:
  port: 8080
  servlet.context-path: /api/

spring:
  liquibase:
    enabled: false
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 50MB
      max-request-size: 50MB